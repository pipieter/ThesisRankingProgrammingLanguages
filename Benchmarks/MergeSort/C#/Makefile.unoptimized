-include ../Makefile.defaults

compile:
	dotnet build unoptimized.csproj -o build/unoptimized/ -c Release --no-incremental

clean:
	dotnet clean

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo build/unoptimized/unoptimized $(ARGS)
