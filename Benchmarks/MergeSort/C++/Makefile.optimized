-include ../Makefile.defaults

compile:
	g++ optimized.cpp -o optimized -O3 -fopenmp -Wall -Wextra -g

clean:
	rm optimized

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo ./optimized $(ARGS)