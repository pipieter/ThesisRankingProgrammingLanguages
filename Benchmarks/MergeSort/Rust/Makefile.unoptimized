-include ../Makefile.defaults

compile:
	cargo build --release --bin unoptimized

clean:
	cargo clean

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo ./target/release/unoptimized $(ARGS)
