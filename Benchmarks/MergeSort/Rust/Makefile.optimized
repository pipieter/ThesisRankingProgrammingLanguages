-include ../Makefile.defaults

compile:
	cargo build --release --bin optimized

clean:
	cargo clean

measure:
ifndef JSON
	$(error No JSON defined.)
else ifndef ARGS
	$(error No ARGS defined.)
endif
	@../../../RAPL/build/rapl --json "$(JSON)" ./target/release/optimized $(ARGS)

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo ./target/release/optimized $(ARGS)
