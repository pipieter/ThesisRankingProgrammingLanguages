-include ../Makefile.defaults

compile:
	cargo build --release --target-dir out/rust --manifest-path Rust/Cargo.toml

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo ./out/rust/release/ionumber $(ARGS)
