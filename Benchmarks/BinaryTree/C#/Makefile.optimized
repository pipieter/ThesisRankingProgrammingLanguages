-include ../Makefile.defaults

compile:
	dotnet build binarytree.csproj -o build/optimized -c Release --no-incremental

clean:
	dotnet clean

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo ./build/optimized/binarytree $(ARGS)
