-include ../Makefile.defaults

CC := g++
CFLAGS := -O3 -fopenmp
OUT := ./optimized

compile:
	$(CC) optimized.cpp -o $(OUT) $(CFLAGS)

clean:
	rm $(OUT)

command:
ifndef ARGS
	$(error No ARGS defined.)
endif
	@echo $(OUT) $(ARGS)